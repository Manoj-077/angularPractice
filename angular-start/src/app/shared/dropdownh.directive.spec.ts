import { DropdownhDirective } from './dropdownh.directive';

describe('DropdownhDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownhDirective();
    expect(directive).toBeTruthy();
  });
});
